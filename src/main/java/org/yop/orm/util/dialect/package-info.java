/**
 * Here are some {@link org.yop.orm.util.ORMTypes} extensions for several SQL DBMS dialects.
 * <br>
 * They are required for schema generation and should only be used for this purpose only !
 * <br>
 * Yop tries to generate some very basic SQL CRUD queries that does not rely on an SQL dialect.
 */
package org.yop.orm.util.dialect;