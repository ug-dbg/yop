/**
 * Here are some {@link org.yop.orm.sql.dialect.IDialect} extensions for several SQL DBMS dialects.
 * <br>
 * They are required for schema generation.
 * <br>
 * However, Yop tries to generate some very basic SQL CRUD queries that does not rely on an SQL dialect.
 */
package org.yop.orm.sql.dialect;